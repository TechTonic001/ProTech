{
  "version": 2,
  "installCommand": "cd Backend && npm install",
  "buildCommand": "echo 'Backend deployment: no build step required'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
