# Local dev only — overrides .env so login hits your running backend
# Use localhost so requests work on THIS machine. For other LAN devices, change to your local IP.
VITE_API_URL=http://localhost:5001/api/
